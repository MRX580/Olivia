version: '3.7'

services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - .:/app
      - users_db_volume:/app/telegram_bot/utils
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"

volumes:
  users_db_volume:
